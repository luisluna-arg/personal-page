# Performance optimizations
prefer-offline=true
audit=false
fund=false
loglevel=error
progress=false
legacy-peer-deps=true

# Increase network concurrency
maxsockets=50

# Registry (use a faster mirror if needed)
# registry=https://registry.npmmirror.com
